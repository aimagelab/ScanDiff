# https://wandb.ai

wandb:
  _target_: src.logging.wandb_logger.WandbLogger
  # name: "" # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  id: null # pass correct id to resume experiment!
  project: "ScanDiff"
  entity: <your team> # set to name of your wandb team
  name: ${list_to_str:${tags}}
